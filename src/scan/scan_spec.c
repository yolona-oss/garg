#include <stdio.h>

#include "util.h"
#include "scan.h"

char *
scan_steam(const char *path, const char *null)
{
	char *ret = NULL;
	char *common = cat_fnames(path, "steamapps/common");

	return ret;
}
